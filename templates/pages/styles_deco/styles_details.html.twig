{% extends 'base.html.twig' %}

{% block content %}

    {% set Apps= theme.theme %}





    <!--                                                 -Bandeau supérieur   / image de font 1894x359 px                                           -->

    <section class="page-header" style="background-image: url(images/backgrounds/main-slider-11.jpg);">
        <div class="page-header-border"></div>
        <div class="page-header-border page-header-border-two"></div>
        <div class="page-header-border page-header-border-three"></div>
        <div class="page-header-border page-header-border-four"></div>
        <div class="page-header-border page-header-border-five"></div>
        <div class="page-header-border page-header-border-six"></div>
        <div class="container">
            <div class="page-header__inner">
                <h2>Styles de décorations</h2>
                <ul class="thm-breadcrumb list-unstyled">
                    <li><a href="{{ path('app_home') }}">Home</a></li>
                    <li><span>/</span></li>
                    <li>Styles en détails</li>
                </ul>
            </div>
        </div>
    </section>
    <!--                                                                fin bandeau supérieur                                                                   -->

    <!--                                                        SECTION  COMTEMPORAIN  / IMGAGE 1170 X 505                                                    -->
    <section class="portfolio-details">

            {% if styled.image.first %}
                {% set imageGd = vich_uploader_asset(styled.image.first) %}
                {% set imageTitle = styled.image.first.titre %}
            {% else %}
                {% set imageGd = asset('assets/images/backgrounds/fond2.png') %}
                {% set imageTitle = 'style en détails et par image' %}
            {% endif %}

            <div class="container">
                <div class="row">
                    <div class="col-xl-12">
                        <div class="portfolio-details__img">
                            <img src="{{ imageGd }}" alt="{{ imageTitle }}" style="max-height:50%; object-fit: cover">
                            <div class="portfolio-details__info">
                                <ul class="portfolio-details__info-list list-unstyled">
                                    <li>{{ styled.attribut1 }}</li>
                                    <li>{{ styled.attribut2 }}</li>
                                    <li>{{ styled.attribut1 }}</li>
                                    <li>
                                        <div class="portfolio-details__social">
                                            <a href="#"><i class="fab fa-twitter"></i></a>
                                            <a href="#" class="clr-fb"><i class="fab fa-facebook"></i></a>
                                            <a href="#" class="clr-dri"><i class="fab fa-dribbble"></i></a>
                                            <a href="#" class="clr-ins"><i class="fab fa-instagram"></i></a>
                                        </div>
                                    </li>
                                </ul>
                            </div>
                        </div>
                        <div class="portfolio-details__content">
                            <h2 class="portfolio-details__title" >{{ styled.titre1 |default('exemple') }} {{ styled.titre2|default('type de décoration') }}  </h2>
                            <p class="portfolio-details__text-1">{{ styled.texte1 }}</p>
                            <ul class="portfolio-details__list list-unstyled">
                                {% set list = styled.liste %}
                                {% for list in list %}
                                <li>
                                    <div class="icon">
                                        <span class="icon-tick"></span>
                                    </div>
                                    <div class="text-box">
                                        <p>{{ list|striptags|default('Sed ut perspiciatis unde omnis iste natus error')|raw}}</p>
                                    </div>
                                </li>
                                {% endfor %}
                                <p class="portfolio-details__text-2 mt-5">{{styled.texte2|raw  }}</p>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
    </section>

<section class="portfolio-details">
    <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">


{#            {% set imageC1 = vich_uploader_asset(styled.image[0]) %}#}
{#            {% set imageTitleC1 = styled.image.titre %}#}
{#            {% else %}#}
{#                {% set imageC1 = vich_uploader_asset(styled.image.first) %}#}
{#                {% set imageTitleC1 = styled.image.first.titre %}#}


{#            {% if styled.image[1] %}#}
{#            {% set imageC1 = vich_uploader_asset(styled.image[1]) %}#}
{#            {% set imageTitleC1 = styled.image[1].titre %}#}
{#            {% else %}#}
{#            {% set imageC1 = vich_uploader_asset(styled.image.first) %}#}
{#            {% set imageTitleC1 = styled.image.first.titre %}#}
{#            {% endif %}#}

            {% if styled.image.first%}
            <div class="carousel-item active">
                <img class="d-block w-100" src="{{ vich_uploader_asset(styled.image[1])}}" alt="First slide" style="max-width: 300px; object-fit: contain">
            </div>
            {% endif %}

            {% if styled.image[1]%}
            <div class="carousel-item">
                <img class="d-block w-100" src="{{ vich_uploader_asset(styled.image[2])}}" alt="Second slide"  style="max-width: 300px; object-fit: contain">
            </div>
            {% endif %}



{#                {% if styled.image[3] %}#}
{#                <div class="carousel-item">#}
{#                    <img class="d-block w-100" src="{{ vich_uploader_asset(styled.image[3]) }}" alt="Third slide"#}
{#                         style="max-width: 300px; object-fit: contain">#}
{#                </div>#}
{#                {% else %}#}
{#                <div class="carousel-item">#}
{#                <img class="d-block w-100" src="{{ vich_uploader_asset(styled.image.first) }}" alt="Third slide"#}
{#                     style="max-width: 300px; object-fit: contain">#}
{#                </div>#}
{#                {% endif %}#}
            </div>



        <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>


</section>


{% endblock %}
