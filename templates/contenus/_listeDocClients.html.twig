{% set Apps = theme.theme %}

{% block content %}
    {#  {% if  documentIdArtisan is defined %} #}
    <div class="alert-primary bg-light mt-0 m-4 mb-3">
        <h2 class="text-truncate mb-2  border-bottom text-center">Liste de vos documents partagés</h2>

        <!-- Liste des documents-->
        <div class="table-responsive">
            <table class="border-bottom m-1 border-bottom mt-xl-5 w-100">
                <thead>
                <tr class=text-center">
                    <th scope="col" class="text-truncate"><i> N°</i></th>
                    <th scope="col" class="text-truncate"><i> Type </i></th>
                    <th scope="col" class="text-truncate">Titre</th>
                    <th scope="col" class="text-truncate">Localité</th>
                    <th scope="col" class="text-center text-truncate">Client 1</th>
                    <th scope="col" class="text-center text-truncater">Client 2</th>
                </tr>
                </thead>
                {% if documentIdClient is defined %}
                    {% for key, tableDoc in documentIdClient %}
                        <tbody>
                        <tr>
                            <th scope="row">{{ key }}</th>
                            <td class="m-lg-5 text-truncate">{{ tableDoc.type }}</td>
                            <td class="m-lg-5 text-truncate p-3">{{ tableDoc.titre }}</td>
                            <td class="m-lg-5 text-truncate ">{{ tableDoc.projet.localite }}</td>
                            {% set clientList=tableDoc.projet.client %}
                            {% for client in clientList %}
                                <td class="m-lg-5 text-truncate text-center">{{ client.prenom }} <br> {{ client.nom }}
                                </td>
                            {% endfor %}
                        </tr>
                        </tbody>
                    {% endfor %}
                {% endif %}
            </table>
        </div>
    </div>
    <!-- Card des documents fonctionnel- A DECOMMENTER SI BESOIN  -->
    {#    <section class="h-auto"> #}
    {#        <div class="container"> #}
    {#            <div class="row align-content-around"> #}
    {#                {% if documentIdArtisan is defined %} #}
    {#                {% for key, arrayCard in documentIdArtisan %} #}
    {#                    <div class="col-sm-4"> #}
    {#                        <div class="card m-3"> #}
    {#                            <div class="card-body m-2" style="height: 500px ; object-fit: contain"> #}
    {#                                <img src="{{ asset('/assets/images/blog_scandinav.png') }}..." class="card-img-top" #}
    {#                                     alt="..."> #}
    {#                                <h5 class="card-title mt-3"><i>N°{{ key }}</i> {{ arrayCard.titre|u.truncate(50,'...',false)|raw }}</h5> #}

    {#                                {% set clientList=arrayCard.projet.client %} #}
    {#                                {% for client in clientList %} #}
    {#                                    <p class="card-text">{{ client.prenom }} {{ client.nom }}</p> #}
    {#                                    <p class="card-text">{{ arrayCard.projet.localite }}</p> #}
    {#                              {% endfor %} #}

    {#                                <a href="#" class="btn btn-primary">Visualiser le document</a> #}
    {#                            </div> #}
    {#                        </div> #}
    {#                    </div> #}
    {#                {% endfor %} #}
    {#                {% endif %} #}
    {#            </div> #}
    {#        </div> #}
    {#    </section> #}
    {# {% endif %} #}
{% endblock %}